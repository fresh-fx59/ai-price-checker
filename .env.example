# Price Monitor Docker Environment Configuration
# Copy this file to .env and customize the values for your deployment

# =============================================================================
# API Configuration
# =============================================================================
# Port to expose the API on the host machine
API_PORT=8443

# Flask environment (development, production)
FLASK_ENV=production

# =============================================================================
# Application Configuration
# =============================================================================
# Path to configuration file inside container
CONFIG_PATH=/app/config/default.properties

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Price check frequency in hours (24 = daily)
CHECK_FREQUENCY_HOURS=24

# Time of day to run daily checks (HH:MM format)
CHECK_TIME=09:00

# Enable mTLS authentication (true/false)
ENABLE_MTLS=true

# Request timeout in seconds
REQUEST_TIMEOUT_SECONDS=30

# Maximum retry attempts for failed requests
MAX_RETRY_ATTEMPTS=3

# =============================================================================
# Email Configuration
# =============================================================================
# SMTP server settings (leave empty to use config file values)
SMTP_SERVER=
SMTP_PORT=
SMTP_USERNAME=
SMTP_PASSWORD=
RECIPIENT_EMAIL=

# =============================================================================
# AI/Parsing Configuration
# =============================================================================
# AI API settings for enhanced product parsing (optional)
AI_API_KEY=
AI_API_ENDPOINT=
ENABLE_AI_PARSING=false

# =============================================================================
# Security Notes
# =============================================================================
# - Keep sensitive values like SMTP_PASSWORD and AI_API_KEY secure
# - Use Docker secrets or external secret management for production
# - Ensure certificate files in ./certs/ have proper permissions (600)
# - Configuration files should be readable but not writable by container